# Architecture Overview

High-level view of MeshBBS components:

- BBS core (server, sessions, commands)
- Meshtastic interface (serial framing, protobuf)
- Storage (messages, users, topic config)
- Config (settings and defaults)

See the library crate docs (rustdoc) for module-level details.
