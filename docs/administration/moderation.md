# Moderation Tools

Moderation commands are available to level 5+ users.

- `DELLOG` / `DL [page]` — view deletion audit log
- `DELETE` — delete a thread
- `LOCK` / `UNLOCK` — toggle topic lock
- `P<n>` — pin/unpin in Threads; `R<n> <title>` rename
